





---
import MainLayout from '../layouts/main.astro';
import { baseUrl } from '../lib/base-url';

const pageClass = 'platform';
---

<MainLayout pageClass={pageClass}>
<!-- HERO -->
<header class="hero" id="top">
  <div class="container">
    <div class="hero-grid">
      <div class="reveal">
        <div class="pill">TalentOS™ Platform</div>
        <h1>Real-time visibility for your contractor and recruiting engine</h1>
        <p class="subhead">
          TalentOS™ is the clean, client-facing window into pipelines, approvals, compliance posture, and program health.
          Less spreadsheet energy, more operator control.
        </p>

        <div class="tabs" role="tablist" aria-label="Platform highlights">
          <button class="tab active" data-tab="dash" role="tab" aria-selected="true">Dashboards</button>
          <button class="tab" data-tab="approvals" role="tab" aria-selected="false">Approvals</button>
          <button class="tab" data-tab="compliance" role="tab" aria-selected="false">Compliance</button>
          <button class="tab" data-tab="reporting" role="tab" aria-selected="false">Reporting</button>
        </div>

        <div class="cta-row">
          <a class="btn primary" href="#get-started">Get a walkthrough</a>
          <a class="btn" href="#workflows">See workflows</a>
        </div>

        <div style="margin-top:14px; color:var(--text-muted); font-size:13px; max-width:80ch;">
          Designed to plug into your reality - Greenhouse, BambooHR, ADP TotalSource, Deel, and your existing cadence.
        </div>
      </div>

      <div class="product reveal" role="img" aria-label="TalentOS mock dashboard">
        <div class="product-top">
          <div class="dots" aria-hidden="true">
            <span class="dot"></span><span class="dot"></span><span class="dot"></span>
          </div>
          <div id="productTitle">TalentOS™ - Dashboard view</div>
          <div class="chip"><span class="spark good"></span> Live</div>
        </div>

        <div class="product-body">
          <div class="ui-grid">
            <div class="ui-card">
              <div class="ui-title">
                <span id="kpi1Label">Open roles</span>
                <span class="chip"><span class="spark"></span><span id="kpi1Tag">Active</span></span>
              </div>
              <div class="ui-value" id="kpi1Value">42</div>
            </div>

            <div class="ui-card">
              <div class="ui-title">
                <span id="kpi2Label">Compliance health</span>
                <span class="chip"><span class="spark good"></span><span id="kpi2Tag">Good</span></span>
              </div>
              <div class="ui-value" id="kpi2Value">98%</div>
            </div>

            <div class="ui-card ui-wide">
              <div class="ui-title">
                <span id="wideLabel">Pipeline velocity</span>
                <span class="chip"><span class="spark"></span><span id="wideTag">Weekly</span></span>
              </div>
              <div class="bars" aria-hidden="true">
                <div class="bar"><span data-w="64%"></span></div>
                <div class="bar"><span data-w="42%"></span></div>
                <div class="bar"><span data-w="78%"></span></div>
                <div class="bar"><span data-w="55%"></span></div>
              </div>
              <div id="wideNote" style="margin-top:12px; color:var(--text-muted); font-size:12px;">
                Intake - shortlist - interview - offer - start (single workflow).
              </div>
            </div>

            <div class="ui-card">
              <div class="ui-title">
                <span id="kpi3Label">Employee satisfaction</span>
                <span class="chip"><span class="spark good"></span><span id="kpi3Tag">4.8/5</span></span>
              </div>
              <div class="ui-value" id="kpi3Value">4.8</div>
            </div>

            <div class="ui-card">
              <div class="ui-title">
                <span id="kpi4Label">Submission to interview</span>
                <span class="chip"><span class="spark"></span><span id="kpi4Tag">Target</span></span>
              </div>
              <div class="ui-value" id="kpi4Value">30-40%</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- WHAT YOU SEE (LIGHT) -->
<section class="section light" id="what-you-see">
  <div class="container">
    <p class="kicker reveal">What you get</p>
    <h2 class="reveal">A shared view that keeps everyone aligned</h2>
    <p class="lead reveal">
      TalentOS™ is built to answer the questions stakeholders ask every week - what is open, what is moving, what is stuck, what is at risk, and what we are doing about it.
    </p>

    <div class="features">
      <div class="feature reveal">
        <div class="f-title">Role dashboards</div>
        <div class="f-body">Open roles, stage movement, and priority ordering with a clean view for hiring managers.</div>
        <div class="f-proof">Outcome: fewer status calls and fewer spreadsheets.</div>
      </div>
      <div class="feature reveal">
        <div class="f-title">Candidate signal</div>
        <div class="f-body">Consistent screening notes, calibrated submissions, and context that helps hiring teams decide faster.</div>
        <div class="f-proof">Outcome: stronger submission to interview conversion.</div>
      </div>
      <div class="feature reveal">
        <div class="f-title">Program health</div>
        <div class="f-body">Visibility into throughput, bottlenecks, aging, and renewals so leaders can plan ahead.</div>
        <div class="f-proof">Outcome: proactive planning, less fire drilling.</div>
      </div>
      <div class="feature reveal">
        <div class="f-title">Consultant tracking</div>
        <div class="f-body">Who is on assignment, end dates, extensions, and location coverage in one view.</div>
        <div class="f-proof">Outcome: smoother renewals and forecasting.</div>
      </div>
      <div class="feature reveal">
        <div class="f-title">Audit-friendly workflow</div>
        <div class="f-body">Structured intake, approvals, and documentation tracking that reduces compliance anxiety.</div>
        <div class="f-proof">Outcome: cleaner governance and risk posture.</div>
      </div>
      <div class="feature reveal">
        <div class="f-title">Fast feedback loops</div>
        <div class="f-body">Managers approve, decline, or refine with minimal friction so the engine keeps moving.</div>
        <div class="f-proof">Outcome: more velocity, less back and forth.</div>
      </div>
    </div>
  </div>
</section>

<!-- WORKFLOWS -->
<section class="section" id="workflows">
  <div class="container">
    <p class="kicker reveal">Workflows</p>
    <h2 class="reveal">One workflow from intake to start</h2>
    <p class="lead reveal">
      Repeatable hiring workflows are how you scale quality. TalentOS™ keeps the path clear, and your program stays predictable.
    </p>

    <div class="flow reveal" role="region" aria-label="Workflow steps">
      <div class="flow-row">
        <span class="flow-dot" aria-hidden="true"></span>
        <div class="flow-tag">
          <div class="n">01</div>
          <div class="t">Intake</div>
        </div>
        <div>
          <h3>Structured intake and calibration</h3>
          <p>Align on must-haves, deal breakers, interview loop, and timeline. No guessing, no mismatched expectations.</p>
        </div>
      </div>

      <div class="flow-row">
        <span class="flow-dot" aria-hidden="true"></span>
        <div class="flow-tag">
          <div class="n">02</div>
          <div class="t">Signal</div>
        </div>
        <div>
          <h3>Screening that improves conversion</h3>
          <p>We submit fewer candidates, but with better fit and clearer context. That is how you drive 30-40% submission to interview.</p>
        </div>
      </div>

      <div class="flow-row">
        <span class="flow-dot" aria-hidden="true"></span>
        <div class="flow-tag">
          <div class="n">03</div>
          <div class="t">Decide</div>
        </div>
        <div>
          <h3>Manager feedback with fewer clicks</h3>
          <p>Approve, decline, or refine. The engine gets direction, and the pipeline stays tight.</p>
        </div>
      </div>

      <div class="flow-row">
        <span class="flow-dot" aria-hidden="true"></span>
        <div class="flow-tag">
          <div class="n">04</div>
          <div class="t">Offer</div>
        </div>
        <div>
          <h3>Offer flow with clean handoffs</h3>
          <p>Clear steps for offers, start dates, and onboarding - so nothing gets lost between stakeholders.</p>
        </div>
      </div>

      <div class="flow-row">
        <span class="flow-dot" aria-hidden="true"></span>
        <div class="flow-tag">
          <div class="n">05</div>
          <div class="t">Start</div>
        </div>
        <div>
          <h3>Onboarding, compliance, and support</h3>
          <p>Background checks, documentation, payroll setup, and consultant support - visible and trackable.</p>
        </div>
      </div>
    </div>

    <div class="grid-2">
      <div class="panel reveal">
        <p class="kicker">Submission to interview</p>
        <h3 style="font-size:18px; margin-bottom:10px;">What "good" looks like</h3>
        <p class="lead">
          A strong, calibrated program commonly lands in the 30-40% range for submission to interview, depending on role complexity and hiring manager discipline.
        </p>
        <ul class="bullets">
          <li>High-volume roles: 25-35% is solid</li>
          <li>Specialized technical roles: 30-45% is strong</li>
          <li>Executive or niche: depends on market, but quality should stay high</li>
        </ul>
        <div class="note" style="margin-top:12px;">If conversion drops, the fix is usually calibration, not more volume.</div>
      </div>

      <div class="panel reveal">
        <p class="kicker">Experience</p>
        <h3 style="font-size:18px; margin-bottom:10px;">Human-led, consistent, measurable</h3>
        <p class="lead">
          We track satisfaction and performance because brand is not a vibe, it is a lived experience.
        </p>
        <ul class="bullets">
          <li>Employee satisfaction: 4.8 out of 5</li>
          <li>Clean communication and predictable next steps</li>
          <li>Manager-friendly summaries that reduce time to decision</li>
        </ul>
        <div class="note" style="margin-top:12px;">You can swap these into verified numbers once you finalize your tracking.</div>
      </div>
    </div>
  </div>
</section>

<!-- SECURITY + INTEGRATIONS (LIGHT) -->
<section class="section light" id="security">
  <div class="container">
    <p class="kicker reveal">Security and compliance</p>
    <h2 class="reveal">Enterprise-ready privacy posture</h2>
    <p class="lead reveal">
      TalentOS™ is designed to keep access clean, data handled responsibly, and workflows audit-friendly. GDPR-aligned by design.
    </p>

    <div class="grid-2">
      <div class="panel reveal">
        <p class="kicker">Security</p>
        <h3 style="font-size:18px; margin-bottom:10px;">Role-based access and clear visibility</h3>
        <ul class="bullets">
          <li>Role-based access controls for stakeholders</li>
          <li>Data minimization by design, share only what is needed</li>
          <li>Consistent documentation tracking for onboarding</li>
          <li>Client-level data segregation across all systems</li>
        </ul>
      </div>

      <div class="panel reveal">
        <p class="kicker">Compliance</p>
        <h3 style="font-size:18px; margin-bottom:10px;">Aligned with global privacy expectations</h3>
        <ul class="bullets">
          <li>GDPR-aware client-facing views and data handling</li>
          <li>Engagement clarity across W2 and global contractor models</li>
          <li>Program health reporting to surface risk early</li>
          <li>Data subject rights support through systems of record</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- TECH STACK -->
<section class="section" id="tech-stack">
  <div class="container">
    <p class="kicker reveal">Technology Stack</p>
    <h2 class="reveal">Where Technology Meets Human-Centric Recruiting</h2>
    <p class="lead reveal">
      TalentOS™ connects our recruiting teams, your hiring managers, and an integrated HR tech stack into one operating system. We use AI to accelerate sourcing, screening, and calibration, while keeping every interaction human, personal, and aligned to your culture.
    </p>

    <div class="stack-layers">
      <!-- Client Experience Layer -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
            </svg>
          </div>
          <div>
            <h3>Client & Talent Experience</h3>
            <p>Visibility, profiles, approvals, and communication</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">Candidately</div>
            <div class="tech-desc">White-labeled client portal • Talent profiles • CXO Spotlight • Shortlists & approvals</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">LinkedIn</div>
            <div class="tech-desc">Sourcing • Employer brand • Talent intelligence • Market mapping</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Email & Calendar</div>
            <div class="tech-desc">Outlook • Gmail • Google Calendar • Candidate scheduling</div>
          </div>
        </div>
      </div>

      <!-- Core Operations: CRM & Recruiting -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
            </svg>
          </div>
          <div>
            <h3>CRM & Recruiting</h3>
            <p>Lead management, sales pipeline, and candidate workflows</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">HubSpot</div>
            <div class="tech-desc">CRM • Lead management • Sales pipeline • Email sequences • Deal tracking</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Greenhouse</div>
            <div class="tech-desc">ATS • Job creation & approvals • Candidate pipeline • Interview workflows • Reporting</div>
          </div>
        </div>
      </div>

      <!-- HRIS & Onboarding -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          </div>
          <div>
            <h3>HRIS & Onboarding</h3>
            <p>New hire onboarding, compliance, and documentation</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">BambooHR</div>
            <div class="tech-desc">New hire onboarding • I-9 + compliance • Employment agreements • Contractor documentation</div>
          </div>
        </div>
      </div>

      <!-- Employment & Payroll -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div>
            <h3>Payroll, Benefits & EOR</h3>
            <p>U.S. and global workforce solutions</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">ADP TotalSource</div>
            <div class="tech-desc">U.S. payroll • Benefits administration • W2 compliance • Time tracking</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Deel</div>
            <div class="tech-desc">Global EOR • International payroll & compliance • Time tracking • 150+ countries</div>
          </div>
        </div>
      </div>

      <!-- Finance & Contracts -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <div>
            <h3>Finance & Contracts</h3>
            <p>Invoicing, payments, and legal documentation</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">QuickBooks</div>
            <div class="tech-desc">Finance • Invoicing • Revenue tracking • Contractor payments</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">DocuSign</div>
            <div class="tech-desc">E-signatures • MSAs • SOWs • Contractor agreements</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Accurate</div>
            <div class="tech-desc">Background checks • Identity verification • Compliance screening</div>
          </div>
        </div>
      </div>

      <!-- Automation & Integrations -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <div>
            <h3>Automation & Integrations</h3>
            <p>Workflow automation and system synchronization</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">Zapier</div>
            <div class="tech-desc">HubSpot ↔ Greenhouse • HubSpot ↔ BambooHR • BambooHR ↔ ADP • Deel ↔ HubSpot • Slack notifications</div>
          </div>
        </div>
      </div>

      <!-- Communication & Collaboration -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
            </svg>
          </div>
          <div>
            <h3>Communication & Collaboration</h3>
            <p>Internal and external communication tools</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">Slack</div>
            <div class="tech-desc">Internal team communication • Notifications • Channel-based collaboration</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Outlook / Gmail</div>
            <div class="tech-desc">External email communication • Client correspondence • Candidate outreach</div>
          </div>
        </div>
      </div>

      <!-- Operations & Task Management -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" />
            </svg>
          </div>
          <div>
            <h3>Task & Project Management</h3>
            <p>Internal workflows, SOPs, and documentation</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">HR Task Tracker</div>
            <div class="tech-desc">Internal task management • Workflow coordination • Process tracking</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Google Drive</div>
            <div class="tech-desc">SOPs • Playbooks • Shared assets • Documentation repository</div>
          </div>
        </div>
      </div>

      <!-- AI & Intelligence -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
            </svg>
          </div>
          <div>
            <h3>AI & Talent Intelligence</h3>
            <p>AI-assisted sourcing, screening, and insights</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">Talent AI</div>
            <div class="tech-desc">AI-assisted sourcing • Resume analysis • Talent matching • Market mapping</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Data & Insights</div>
            <div class="tech-desc">Client-specific KPIs • Talent funnel analytics • Time-to-fill • Quality metrics</div>
          </div>
        </div>
      </div>

      <!-- Website & Marketing -->
      <div class="stack-layer reveal">
        <div class="layer-header">
          <div class="layer-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <div>
            <h3>Website & Marketing</h3>
            <p>Digital presence, analytics, and content</p>
          </div>
        </div>
        <div class="tech-grid">
          <div class="tech-card">
            <div class="tech-name">Webflow</div>
            <div class="tech-desc">Website CMS • Sustainable Talent • Scrambler Therapy • Disc Therapy Institute</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Google Analytics</div>
            <div class="tech-desc">Website analytics • Traffic insights • User behavior tracking</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">Google Ads</div>
            <div class="tech-desc">Paid search campaigns • Lead generation • Performance marketing</div>
          </div>
          <div class="tech-card">
            <div class="tech-name">YouTube</div>
            <div class="tech-desc">Video content • Thought leadership • Employer brand storytelling</div>
          </div>
        </div>
      </div>
    </div>

    <div class="stack-footer reveal">
      <p><strong>Enterprise-Ready Architecture:</strong> All systems are modular, secure, and designed to integrate seamlessly with client environments. Our stack supports everything from a single hire to global, multi-country workforces.</p>
      <p style="margin-top:10px;"><strong>GDPR Compliant:</strong> Data protection by design with role-based access, minimal data exposure, and data subject rights support through systems of record.</p>
      <p style="margin-top:10px;"><strong>One-liner:</strong> Sustainable Talent operates a modern, AI-enabled talent operating system built on Greenhouse, HubSpot, BambooHR, ADP, Deel, and a proprietary client portal — delivering enterprise-grade workforce solutions with a human-first approach.</p>
    </div>

    <div class="cta-row" style="margin-top:28px; justify-content:center;">
      <a class="btn primary" href="#get-started">Book A Call</a>
      <a class="btn" href={`${baseUrl}/#contact`}>Contact Us</a>
    </div>
  </div>
</section>

<!-- INTEGRATION TRUST -->
<section class="section" id="integrations">
  <div class="container">
    <p class="kicker reveal">Integration Philosophy</p>
    <h2 class="reveal">Designed to Work With Your Stack</h2>
    <p class="lead reveal">
      Sustainable Talent is built to integrate into your existing systems, not disrupt them.
      Whether you're operating on an enterprise HRIS, a modern ATS, or a custom internal workflow,
      our platform connects through secure integrations, structured exports, and approval-ready reporting.
      We meet clients where they are – operationally and technically.
    </p>

    <div class="integration-methods">
      <div class="int-card reveal">
        <div class="int-icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 4a2 2 0 114 0v1a1 1 0 001 1h3a1 1 0 011 1v3a1 1 0 01-1 1h-1a2 2 0 100 4h1a1 1 0 011 1v3a1 1 0 01-1 1H7a1 1 0 01-1-1v-1a2 2 0 10-4 0v1a1 1 0 01-1 1H4a2 2 0 110-4h1a1 1 0 001-1V7a1 1 0 011-1h3a1 1 0 001-1V4z" />
          </svg>
        </div>
        <div>
          <h3>Native Integrations</h3>
          <p class="lead" style="margin-top:8px;">Seamless connections with the tools you already use.</p>
          <ul class="bullets" style="margin-top:14px;">
            <li>ATS and HR platforms (Greenhouse, BambooHR)</li>
            <li>Payroll and benefits providers (ADP, Deel)</li>
            <li>Calendar and email systems (Outlook, Gmail)</li>
            <li>Communication tools (Slack, Teams)</li>
          </ul>
        </div>
      </div>

      <div class="int-card reveal">
        <div class="int-icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
        </div>
        <div>
          <h3>Secure Data Sync</h3>
          <p class="lead" style="margin-top:8px;">Enterprise-grade security for all system connections.</p>
          <ul class="bullets" style="margin-top:14px;">
            <li>API-based automation with Zapier and direct integrations</li>
            <li>Role-based access controls across all platforms</li>
            <li>Client-level data separation and segregation</li>
            <li>Encrypted data transmission and storage</li>
          </ul>
        </div>
      </div>

      <div class="int-card reveal">
        <div class="int-icon" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-2m3 2v-4m3 4v-6m2 10H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
        </div>
        <div>
          <h3>Enterprise Exports</h3>
          <p class="lead" style="margin-top:8px;">Clean, structured data ready for your systems.</p>
          <ul class="bullets" style="margin-top:14px;">
            <li>Time tracking approvals and timesheets</li>
            <li>Hiring and talent pipeline reports</li>
            <li>Compliance-ready documentation and audits</li>
            <li>Custom report formats to match your needs</li>
          </ul>
        </div>
      </div>
    </div>

    <div class="closing-line reveal" style="margin-top:32px; text-align:center;">
      <p style="font-size:16px; color:var(--text-primary); font-weight:600;">
        Our goal is operational alignment – not another tool your team has to manage.
      </p>
    </div>
  </div>
</section>

<!-- SECURITY DEEP DIVE -->
<section class="section light" id="security-detail">
  <div class="container">
    <p class="kicker reveal">Security & Compliance</p>
    <h2 class="reveal">Enterprise-Grade Standards</h2>
    <p class="lead reveal">
      Sustainable Talent operates on proven, enterprise-grade platforms trusted by global organizations.
      Our infrastructure prioritizes data security, compliance, and operational integrity at every stage of the talent lifecycle.
    </p>

    <div class="security-grid">
      <div class="sec-card reveal">
        <div class="sec-header">
          <div class="sec-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
          </div>
          <h3>Data Security</h3>
        </div>
        <ul class="bullets">
          <li>Role-based access controls for all stakeholders</li>
          <li>Client-level data separation across all systems</li>
          <li>Secure authentication and multi-factor authorization</li>
          <li>Encrypted data storage and transmission (TLS 1.2+)</li>
          <li>Regular security audits and vulnerability assessments</li>
          <li>Data minimization by design – share only what's needed</li>
        </ul>
      </div>

      <div class="sec-card reveal">
        <div class="sec-header">
          <div class="sec-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
          </div>
          <h3>Compliance Coverage</h3>
        </div>
        <ul class="bullets">
          <li>U.S. payroll and employment compliance (ADP TotalSource)</li>
          <li>Global contractor and EOR compliance (Deel, 150+ countries)</li>
          <li>I-9, onboarding, and employment documentation management</li>
          <li>Background checks and identity verification (Accurate)</li>
          <li>GDPR-aligned data handling and privacy controls</li>
          <li>Data subject rights support through systems of record</li>
        </ul>
      </div>

      <div class="sec-card reveal">
        <div class="sec-header">
          <div class="sec-icon" aria-hidden="true">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" />
            </svg>
          </div>
          <h3>Operational Controls</h3>
        </div>
        <ul class="bullets">
          <li>Approval-based workflows with documented decision trails</li>
          <li>Audit-ready reporting and exportable activity records</li>
          <li>Time tracking and payroll approval chains</li>
          <li>Clear ownership and accountability across all systems</li>
          <li>Incident response protocols and escalation procedures</li>
          <li>Regular compliance training for all team members</li>
        </ul>
      </div>
    </div>

    <div class="trust-closing reveal" style="margin-top:32px;">
      <div class="trust-box">
        <div class="trust-icon" aria-hidden="true">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
          </svg>
        </div>
        <div>
          <h3 style="margin:0 0 8px; font-size:20px;">Security isn't a feature – it's the foundation of how we operate.</h3>
          <p style="margin:0; color:var(--text-secondary); font-size:15px;">
            Every system we use, every integration we build, and every workflow we design is evaluated through a security-first lens.
            We work with trusted, enterprise-grade vendors because your data and your reputation are on the line.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- FAQ -->
<section class="section" id="faq">
  <div class="container">
    <p class="kicker reveal">FAQ</p>
    <h2 class="reveal">The questions stakeholders actually ask</h2>
    <p class="lead reveal">
      Short, direct answers that reduce sales friction and make you look buttoned up.
    </p>

    <div class="faq">
      <details class="reveal">
        <summary>Is TalentOS™ a replacement for our ATS or HRIS? <span class="caret" aria-hidden="true"></span></summary>
        <p>No. TalentOS™ is the operating and visibility layer for your contractor and recruiting engine. It complements your ATS and HRIS by making the program easier to run and easier to see.</p>
      </details>

      <details class="reveal">
        <summary>What does a typical rollout look like? <span class="caret" aria-hidden="true"></span></summary>
        <p>We start with a structured intake, define workflows and approvals, then activate pipelines. The goal is quick alignment, clean execution, and reporting that stakeholders trust.</p>
      </details>

      <details class="reveal">
        <summary>How do you improve submission to interview conversion? <span class="caret" aria-hidden="true"></span></summary>
        <p>Calibration first. We define what "good" looks like with the hiring team, submit fewer candidates with better context, and tighten feedback loops so the engine learns quickly.</p>
      </details>

      <details class="reveal">
        <summary>Can we customize views by stakeholder? <span class="caret" aria-hidden="true"></span></summary>
        <p>Yes. Different stakeholders need different views. TalentOS™ is designed for role-based visibility so managers, HR, and leadership see what is relevant without clutter.</p>
      </details>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="section light" id="get-started">
  <div class="container">
    <div class="cta-card reveal">
      <div>
        <p class="kicker">Get a walkthrough</p>
        <h2 style="margin-bottom:10px;">See what stakeholders see</h2>
        <p>
          Enter your email and we will send a quick overview of the platform, the model, and what onboarding looks like.
        </p>
        <div class="helper">One thoughtful follow-up. No spam.</div>
      </div>

      <form class="form" action="#" method="post">
        <input class="input" type="email" name="email" placeholder="Enter your email address" required />
        <button class="btn primary" type="submit">Submit</button>
      </form>
    </div>
  </div>
</section>
</MainLayout>

<style>
/* Hero */
.hero {
  padding: 92px 0 46px;
  background: var(--bg-section);
  position: relative;
  overflow: hidden;
}

.hero:before {
  content: "";
  position: absolute;
  inset: -200px -200px auto -200px;
  height: 520px;
  background: linear-gradient(120deg, rgba(99, 102, 241, 0.08), transparent 55%);
  pointer-events: none;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1.08fr 0.92fr;
  gap: 34px;
  align-items: center;
  position: relative;
  z-index: 1;
}

.pill {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 7px 10px;
  border-radius: 999px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.75);
  color: var(--text-secondary);
  font-size: 12px;
  letter-spacing: 0.6px;
  text-transform: uppercase;
  backdrop-filter: blur(8px);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.05);
}

.subhead {
  margin: 0 0 18px;
  color: var(--text-secondary);
  font-size: 18px;
  max-width: 72ch;
}

.cta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  margin-top: 18px;
  align-items: center;
}

/* Tabs */
.tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 18px;
}

.tab {
  border-radius: 999px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.65);
  color: var(--text-secondary);
  padding: 9px 12px;
  font-size: 13px;
  font-weight: 700;
  cursor: pointer;
  transition: transform 0.15s ease, background 0.15s ease, border-color 0.15s ease, color 0.15s ease;
  user-select: none;
  white-space: nowrap;
  backdrop-filter: blur(8px);
}

.tab:hover {
  transform: translateY(-1px);
  border-color: rgba(15, 23, 42, 0.14);
  color: var(--text-primary);
  background: rgba(255, 255, 255, 0.80);
}

.tab.active {
  color: #fff;
  border-color: transparent;
  background: var(--accent);
  box-shadow: 0 12px 24px rgba(99, 102, 241, 0.18);
}

/* Product Frame */
.product {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: var(--bg-glass);
  box-shadow: 0 28px 70px rgba(15, 23, 42, 0.10);
  overflow: hidden;
  position: relative;
  min-height: 380px;
  animation: floaty 7s ease-in-out infinite;
  transform: translateZ(0);
  backdrop-filter: blur(8px);
}

.product:after {
  content: "";
  position: absolute;
  inset: -40%;
  background: conic-gradient(from 180deg,
    rgba(99, 102, 241, 0.00),
    rgba(99, 102, 241, 0.10),
    rgba(56, 189, 248, 0.08),
    rgba(99, 102, 241, 0.00)
  );
  filter: blur(32px);
  opacity: 0.35;
  animation: spin 12s linear infinite;
  pointer-events: none;
  mix-blend-mode: normal;
}

@keyframes floaty {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.product > * {
  position: relative;
  z-index: 1;
}

.product-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  color: var(--text-secondary);
  font-size: 12px;
  padding: 14px 16px;
  border-bottom: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.35);
}

.dots {
  display: flex;
  gap: 7px;
}

.dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: rgba(15, 23, 42, 0.10);
}

.dot:nth-child(1) { background: rgba(15, 23, 42, 0.14); }
.dot:nth-child(2) { background: rgba(15, 23, 42, 0.10); }
.dot:nth-child(3) { background: rgba(15, 23, 42, 0.08); }

.chip {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 6px 10px;
  border-radius: 999px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.55);
  color: var(--text-secondary);
  font-size: 12px;
  white-space: nowrap;
  backdrop-filter: blur(8px);
}

.spark {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: rgba(99, 102, 241, 0.85);
  box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.14);
}

.spark.good {
  background: rgba(34, 197, 94, 0.85);
  box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.14);
}

.product-body {
  padding: 16px;
}

.ui-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.ui-card {
  border-radius: 16px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.55);
  padding: 14px;
  min-height: 96px;
  backdrop-filter: blur(8px);
}

.ui-title {
  font-size: 12px;
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
}

.ui-value {
  margin-top: 10px;
  font-size: 24px;
  font-weight: 800;
  letter-spacing: -0.4px;
  color: var(--text-primary);
}

.ui-wide {
  grid-column: 1 / -1;
  min-height: 170px;
}

.bars {
  margin-top: 12px;
  display: grid;
  gap: 8px;
}

.bar {
  height: 10px;
  border-radius: 999px;
  background: rgba(15, 23, 42, 0.06);
  overflow: hidden;
  border: 1px solid rgba(15, 23, 42, 0.06);
}

.bar > span {
  display: block;
  height: 100%;
  width: 10%;
  background: linear-gradient(90deg, rgba(99, 102, 241, 0.85), rgba(56, 189, 248, 0.70));
  border-radius: 999px;
  transition: width 900ms ease;
}

/* Sections */
.section {
  padding: 96px 0;
  background: var(--bg-section);
}

.section.light {
  position: relative;
  background:
    radial-gradient(1100px 520px at 15% 10%, rgba(99, 102, 241, 0.08), transparent 60%),
    radial-gradient(1100px 520px at 85% 0%, rgba(99, 102, 241, 0.06), transparent 62%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.90), rgba(245, 246, 248, 0.85));
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
}

.kicker {
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.8px;
  font-size: 12px;
  margin: 0 0 10px;
}

.lead {
  margin: 0;
  color: var(--text-secondary);
  font-size: 16px;
  max-width: 86ch;
}

/* Features Grid */
.features {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
  margin-top: 22px;
}

.feature {
  border-radius: 24px;
  border: 1px solid var(--border-light);
  background: var(--bg-glass);
  padding: 16px;
  min-height: 150px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  box-shadow: 0 20px 40px rgba(15, 23, 42, 0.08);
  backdrop-filter: blur(8px);
}

.feature .f-title {
  font-weight: 900;
  letter-spacing: -0.2px;
  color: var(--text-primary);
}

.feature .f-body {
  color: var(--text-secondary);
  font-size: 13px;
}

.feature .f-proof {
  margin-top: auto;
  color: var(--text-muted);
  font-size: 12px;
  border-top: 1px solid var(--border-light);
  padding-top: 10px;
}

/* Flow */
.flow {
  margin-top: 22px;
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: var(--bg-section);
  overflow: hidden;
  box-shadow: 0 20px 40px rgba(15, 23, 42, 0.08);
}

.flow-row {
  display: grid;
  grid-template-columns: 160px 1fr;
  gap: 16px;
  padding: 18px;
  border-top: 1px solid var(--border-light);
  position: relative;
  background: rgba(255, 255, 255, 0.70);
}

.flow-row:first-child {
  border-top: none;
}

.flow-tag {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.flow-tag .n {
  font-weight: 900;
  letter-spacing: 0.8px;
  color: var(--text-primary);
}

.flow-tag .t {
  color: var(--text-muted);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.8px;
}

.flow-row h3 {
  font-size: 16px;
}

.flow-row p {
  margin: 8px 0 0;
  color: var(--text-secondary);
  font-size: 14px;
}

.flow-row:before {
  content: "";
  position: absolute;
  left: 22px;
  top: -1px;
  bottom: -1px;
  width: 2px;
  background: linear-gradient(180deg, rgba(99, 102, 241, 0.40), rgba(56, 189, 248, 0.22));
  opacity: 0.55;
  border-radius: 999px;
}

.flow-dot {
  position: absolute;
  left: 16px;
  top: 22px;
  width: 14px;
  height: 14px;
  border-radius: 999px;
  background: rgba(99, 102, 241, 0.85);
  box-shadow: 0 0 0 6px rgba(99, 102, 241, 0.14);
}

/* Grid */
.grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  align-items: stretch;
  margin-top: 22px;
}

.panel {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: var(--bg-glass);
  padding: 22px;
  box-shadow: 0 20px 40px rgba(15, 23, 42, 0.08);
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(8px);
}

.bullets {
  margin: 14px 0 0;
  padding: 0 0 0 18px;
  color: var(--text-secondary);
}

.bullets li {
  margin: 10px 0;
}

.note {
  color: var(--text-muted);
  font-size: 12px;
}

/* Logos */
.logos {
  margin-top: 18px;
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  align-items: center;
}

.logo-badge {
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.65);
  color: var(--text-secondary);
  font-weight: 700;
  font-size: 12px;
  letter-spacing: 0.2px;
  user-select: none;
  backdrop-filter: blur(8px);
}

/* Tech Stack Section */
.stack-layers {
  margin-top: 32px;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.stack-layer {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 28px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.stack-layer::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.stack-layer:hover {
  transform: translateY(-4px);
  box-shadow: 0 32px 64px rgba(15,23,42,0.12);
  border-color: rgba(99, 102, 241, 0.2);
}

.layer-header {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  margin-bottom: 20px;
}

.layer-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.12), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.06);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.stack-layer:hover .layer-icon {
  transform: translateY(-2px) scale(1.05);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.15);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.18), transparent 65%),
    rgba(255,255,255,0.85);
}

.layer-header h3 {
  font-size: 20px;
  margin: 0 0 4px 0;
  color: var(--text-primary);
}

.layer-header p {
  margin: 0;
  font-size: 14px;
  color: var(--text-muted);
}

.tech-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 12px;
}

.tech-card {
  border-radius: 18px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.70);
  padding: 16px;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  box-shadow: 0 8px 18px rgba(15, 23, 42, 0.06);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

.tech-card:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 16px 32px rgba(15, 23, 42, 0.10);
  border-color: rgba(99, 102, 241, 0.15);
  background: rgba(255, 255, 255, 0.85);
}

.tech-name {
  font-weight: 700;
  font-size: 16px;
  color: var(--text-primary);
  margin-bottom: 6px;
  letter-spacing: -0.2px;
}

.tech-desc {
  font-size: 13px;
  color: var(--text-secondary);
  line-height: 1.5;
}

.stack-footer {
  margin-top: 32px;
  padding: 24px;
  border-radius: 20px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.60);
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.06);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

.stack-footer p {
  margin: 0;
  font-size: 14px;
  color: var(--text-secondary);
}

.stack-footer strong {
  color: var(--text-primary);
  font-weight: 700;
}

/* Integration Methods */
.integration-methods {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.int-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.08), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.70) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.int-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.int-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.int-icon {
  width: 56px;
  height: 56px;
  border-radius: 16px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(40px 40px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 12px 24px rgba(15, 23, 42, 0.08);
  margin-bottom: 18px;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.int-card:hover .int-icon {
  transform: translateY(-3px) scale(1.08);
  box-shadow: 0 16px 32px rgba(99, 102, 241, 0.18);
}

.int-card h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Closing Line */
.closing-line {
  padding: 20px 24px;
  border-radius: 18px;
  border: 1px solid var(--border-light);
  background: rgba(255, 255, 255, 0.70);
  box-shadow: 0 12px 28px rgba(15, 23, 42, 0.08);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

.closing-line p {
  margin: 0;
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 48px;
  height: 48px;
  border-radius: 14px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(30px 30px at 40% 40%, rgba(99,102,241,0.14), transparent 65%),
    rgba(255,255,255,0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--accent);
  box-shadow: 0 10px 22px rgba(15, 23, 42, 0.08);
  flex-shrink: 0;
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card:hover .sec-icon {
  transform: translateY(-2px) scale(1.06);
  box-shadow: 0 14px 28px rgba(99, 102, 241, 0.16);
}

.sec-header h3 {
  font-size: 18px;
  margin: 0;
  color: var(--text-primary);
}

/* Security Grid */
.security-grid {
  margin-top: 28px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}

.sec-card {
  border-radius: 28px;
  border: 1px solid var(--border-light);
  background: 
    radial-gradient(400px 200px at 30% 20%, rgba(99, 102, 241, 0.06), transparent 60%),
    linear-gradient(135deg, rgba(255, 255, 255, 0.85) 0%, rgba(255, 255, 255, 0.65) 100%),
    var(--bg-glass);
  padding: 24px;
  box-shadow: 
    0 20px 40px rgba(15,23,42,0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  position: relative;
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.sec-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, 
    transparent, 
    rgba(99, 102, 241, 0.3) 30%, 
    rgba(56, 189, 248, 0.2) 70%, 
    transparent);
  opacity: 0.7;
}

.sec-card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 
    0 28px 64px rgba(15,23,42,0.12),
    0 0 40px rgba(99, 102, 241, 0.08);
  border-color: rgba(99, 102, 241, 0.2);
}

.sec-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 18px;
}

.sec-icon {
  width: 4